package edu.chalmers.vaporwave.model.menu;

/**
 * Enum for keeping track of which state a menu is in; which screen to show.
 */
public enum MenuState {
    START_MENU,
    ROOSTER,
    CHARACTER_SELECT,
    RESULTS_MENU,
    NO_ACTION,
    EXIT_PROGRAM,
    START_GAME,
    OPTIONS_MENU,
    CONTROLS_MENU
}
